define (function(){

    var pFrameRate = 50,
        pbgHeight = 500,
           pbgWidth = 500,
           psprites = [];

    return {
           frameRate: pFrameRate,
           bgHeight : pbgHeight,
           bgWidth : pbgWidth

    };
});
